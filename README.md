# Southern Sierra Wildflower Club (SSWC) Flower Database

A user interface for the SSWC Flower Database.

To run:

  * Install Node.js dependencies with `npm install`
  * Start Phoenix endpoint with `node server.js`
